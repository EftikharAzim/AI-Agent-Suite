# Secrets & Sensitive Data
# ========================

# User Secrets (local development) - automatically excluded by .gitignore
# but documenting here for clarity
# Windows: %APPDATA%\Microsoft\UserSecrets\
# Linux/macOS: ~/.microsoft/usersecrets/

# Never commit these patterns
*.key
*.pem
secrets.json
secrets.*.json
appsettings.secrets.json

# Azure CLI local cache (may contain credentials)
.azure/

# Environment variable files
.env
.env.local
.env.*.local

# IDE-specific files that might contain secrets
.vscode/settings.json
.idea/

# Application logs (might contain sensitive data)
logs/
*.log

# If appsettings files ever get committed with secrets
# (they shouldn't, but just in case)
appsettings.Production.local.json

# Temporary files
*.tmp
*.temp
